/**
 * Twig Toolbox parser.
 *
 * Placeholder surface until milestone 03 lands the real tolerant lexer/parser.
 */

/** Twig dialect version this parser targets. */
export const TWIG_VERSION = '3.x';

/** Reports whether the parser has a working implementation behind it yet. */
export function isImplemented(): boolean {
	return false;
}
